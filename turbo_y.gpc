int combo_toggle = FALSE;
 
main {
    if (event_press(XB360_BACK)) {
         combo_toggle = !combo_toggle;
    }
 
    if(combo_toggle) {
         combo_run (turbo);
    }
}
 
combo turbo {
    // Press Y
    set_val(XB360_Y, 100);
    wait(50);
 
    // Press Y
    set_val(XB360_Y, 100);
    wait(50);
 
    // Press Y
    set_val(XB360_Y, 100);
    wait(50);
 
    // Press Y
    set_val(XB360_Y, 100);
    wait(50);
 
    // Press Y
    set_val(XB360_Y, 100);
    wait(50);
 
    // Press Y
    set_val(XB360_Y, 100);
    wait(50);
}
/*